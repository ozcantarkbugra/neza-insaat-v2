# Ortam: development | production
APP_ENV=development

# Veritabanı (Docker: localhost:5472)
# Production (Docker Compose): postgresql://postgres:PASSWORD@postgres:5432/construction_db?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5472/construction_db?schema=public"

# JWT (min 32 karakter - production'da mutlaka değiştirin!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Sunucu
NODE_ENV=development
PORT=5002
FRONTEND_URL=http://localhost:3000

# Opsiyonel: Google Maps
# GOOGLE_MAPS_API_KEY=

# Opsiyonel: SMTP (iletişim formu e-posta)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
